# Well-formed and valid schema, but fails extra constraints. The defaults
# and references refer to
kind: 'resource'
kbtype: 'dummyarticle'
based_on: 'article'
defaults:
  style: defaultstyle
references:
  - xxxauthor
---
type: map
mapping:
  style:
    type: str
  author:
    type:str
