type: map
mapping:
  kind:
    type: str
    enum: ['resource', 'widget']
    required: True
  kbtype:
    type: str
    required: True
  based_on:
    type: str
    required: True
  defaults:
    type: map
    allowempty: True
  references:
    type: seq
    sequence:
      - type: str
